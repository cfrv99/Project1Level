let skillContainer=document.querySelector(".leftMenu_skillsContainer"),skills=document.querySelectorAll(".leftMenu_skill");skillContainer.style.columnCount=Math.round(skills.length/2);let imageUploader=document.querySelector(".registration_inputPhoto");imageUploader.addEventListener("change",e=>{document.querySelector(".registration_photo").src=URL.createObjectURL(e.target.files[0])});

let imageUploader = document.querySelector(".registration_inputPhoto"); imageUploader.addEventListener("change", e => { document.querySelector(".registration_photo").src = URL.createObjectURL(e.target.files[0]) });